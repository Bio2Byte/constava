This directory contains:

1. `constava-testdata`: required files to perform the unit test validation via `constava test` command.
1. `constava_csdata.json`: defined as `DEFAULT_TRAINING_DATA_PATH` in the `constants` module.
